// Seed script — run with: npx tsx seed/seed.ts
// Will populate the database with initial data

async function main() {
  console.log('Seeding database...');
  // Seed logic will be added as features are built
  console.log('Seed complete.');
}

main().catch(console.error);
