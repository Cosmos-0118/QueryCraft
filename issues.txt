we do have quite a few issues:

1) we have our own databases right. things like alter and update commands to things like column names doesnt seem to work
reliably.

2) so in a browser if a create a two accounts. their history all all the data is leaking into
one another. they are not seperate accounts.

also we have a major issues in our engines - check and fix it for all engines(alzebra, tuple and mainly mysql).

1) they should absolutely all the my sql commands user throw at them.
2) currently trigger -> only sqllite syntax work not the actual mysql syntax.
3) cursor and all doesnt even work - add a button to see cursor like procedure and trigger too.
4)all the popups like triggers do look good. but their alignmant and clarity could be better.
5) the way all the setting arranged at the top. on smaller screen it feels suffocating and un professonal. use the 
best beautiful layout possible.